node_modules/
dist/
*.tsbuildinfo

# Alternate package managers — this repo uses npm. Prevent accidental
# pnpm/yarn lockfile commits if a contributor uses a different client.
pnpm-lock.yaml
yarn.lock
bun.lockb
bun.lock

# Sniff output (NOT sniff-tests/ — that holds fixtures tests depend on)
.sniff/
sniff-baselines/
sniff-reports/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.*

# Planning (internal)
.planning/

.omc/
PROJECT-BRIEF.md

# crawl-engine dev artifacts (regenerable)
docs/audit/green/screens/
docs/audit/green/*.log

# Demo video build artifacts (scenes, frames, audio, mp4 — regenerable).
# The committed hero asset is .github/assets/demo.gif, which is NOT ignored.
demo-output/
