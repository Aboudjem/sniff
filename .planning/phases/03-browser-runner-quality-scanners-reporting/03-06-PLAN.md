---
phase: 03-browser-runner-quality-scanners-reporting
plan: 06
type: execute
wave: 5
depends_on: ["03-05"]
files_modified: []
autonomous: false
requirements:
  - CLI-03
  - E2E-01
  - E2E-02
  - E2E-03
  - E2E-04
  - E2E-05
  - E2E-06
  - A11Y-01
  - A11Y-02
  - A11Y-03
  - VIS-01
  - VIS-02
  - VIS-03
  - PERF-01
  - PERF-02
  - PERF-03
  - RPT-01
  - RPT-02
  - RPT-03
  - RPT-04
  - RPT-05
  - RPT-06

must_haves:
  truths:
    - "sniff run executes end-to-end without errors on a sample app"
    - "HTML report opens in browser and displays all sections"
    - "JSON and JUnit reports are valid and parseable"
    - "Exit code reflects findings vs severity threshold"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification of the complete Phase 3 implementation. Run `sniff run` against a real or sample application to verify the full pipeline works: browser launch, quality scanning, report generation, terminal output, and exit codes.

Purpose: Validate that all 22 requirements are met by running the integrated system end-to-end with human verification of report quality and visual output.
Output: Verification confirmation or list of issues to fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-browser-runner-quality-scanners-reporting/03-CONTEXT.md
@.planning/phases/03-browser-runner-quality-scanners-reporting/03-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification of compilation, imports, and report output</name>
  <files></files>
  <read_first>
    src/cli/commands/run.ts,
    src/cli/index.ts,
    src/browser/runner.ts,
    src/scanners/accessibility/index.ts,
    src/scanners/visual/index.ts,
    src/scanners/performance/index.ts,
    src/report/html.ts,
    src/report/json.ts,
    src/report/junit.ts,
    src/report/model.ts
  </read_first>
  <action>
Run a sequence of automated checks to verify the integrated system:

1. **TypeScript compilation:** `npx tsc --noEmit` must exit 0.

2. **CLI help text:** Run `node dist/cli/index.js run --help` (after build) and verify it shows `--base-url`, `--no-headless`, `--format`, `--fail-on`, `--json` options. Run `node dist/cli/index.js update-baselines --help` and verify it shows `--yes` option.

3. **Import chain verification:** Verify no circular dependencies by running: `npx madge --circular src/` (if available) or manually check that:
   - `src/browser/` imports from `src/core/`, `src/config/`, `src/scanners/types.ts` (not from `src/cli/`)
   - `src/scanners/accessibility/` imports from `src/scanners/types.ts`, `src/core/types.ts` (not from `src/browser/runner.ts`)
   - `src/report/` imports from `src/core/types.ts`, `src/scanners/types.ts` (not from `src/browser/` or `src/cli/`)
   - `src/cli/commands/run.ts` imports from all other modules (it's the integration point)

4. **Package.json dependencies:** Verify that `playwright`, `@axe-core/playwright`, `pixelmatch`, `pngjs`, `lighthouse`, `chrome-launcher` are listed in package.json (as dependencies, peerDependencies, or optionalDependencies as appropriate). If missing, add them.

5. **Build check:** Run `npm run build` (or `npx tsc`) and verify it produces output in `dist/`.

6. **Dry-run report generation test:** Create a small TypeScript test script that:
   - Imports `buildReport` from `src/report/model.js`
   - Imports `generateJsonReport` from `src/report/json.js`
   - Imports `generateJunitReport` from `src/report/junit.js`
   - Imports `generateHtmlReport` from `src/report/html.js`
   - Creates a mock SniffReport with sample findings across all scanner types
   - Calls each generator and verifies output is non-empty and valid:
     - JSON: `JSON.parse(jsonOutput)` does not throw
     - JUnit: contains `<?xml version` and `<testsuites`
     - HTML: contains `<!DOCTYPE html>` and `Sniff QA Report`
   - Run this test and verify all assertions pass

7. **Fix any issues** discovered during verification.
  </action>
  <verify>
    <automated>cd /Users/adamboudj/projects/sniff && npx tsc --noEmit && echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `node dist/cli/index.js run --help` contains `--base-url`
    - `node dist/cli/index.js update-baselines --help` contains `--yes`
    - package.json contains `playwright` in dependencies or peerDependencies
    - package.json contains `@axe-core/playwright` in dependencies
    - package.json contains `pixelmatch` in dependencies
    - package.json contains `lighthouse` in dependencies or optionalDependencies
    - No circular imports between src/browser, src/scanners, src/report, src/cli
    - Report generators produce valid output from mock data
  </acceptance_criteria>
  <done>Full compilation passes, CLI shows all new options, dependencies declared, no circular imports, report generators produce valid output.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of end-to-end sniff run experience</name>
  <files></files>
  <action>
Present the complete Phase 3 implementation to the user for manual verification. The user will:
1. Start a local dev server for a web app
2. Run `npx sniff run --base-url http://localhost:PORT`
3. Verify terminal output shows progress per viewport and scanner
4. Open the HTML report and verify visual quality, dark mode, brutal tone
5. Verify JSON and JUnit reports are valid
6. Test `sniff update-baselines` with confirmation prompt
7. Re-run to confirm baselines accepted

Display the verification checklist from the how-to-verify section below and wait for user response.
  </action>
  <verify>User types "approved" or provides feedback on issues to fix.</verify>
  <done>User has verified the complete end-to-end experience and approved or provided actionable feedback.</done>
  <what-built>Complete Phase 3 implementation: `sniff run` command that launches Playwright, executes accessibility/visual/performance scanners across viewports, generates HTML/JSON/JUnit reports, and displays terminal output with severity-based findings.</what-built>
  <how-to-verify>
    1. Start a local development server for any web app (e.g., `npx serve .` on a static site, or your own project's dev server)
    2. Run: `npx sniff run --base-url http://localhost:3000` (or your app's port)
    3. Observe terminal output:
       - Should show "Sniff v0.1.0 -- Running quality scan"
       - Should show progress per viewport (desktop, mobile, tablet)
       - Should show scanner progress ([a11y], [visual], [perf])
       - Should show findings summary with severity colors
       - Should print report file paths
    4. Open the generated HTML report (path shown in terminal output)
       - Verify it has sections: Browser Tests, Accessibility, Visual Regression, Performance
       - Verify finding cards have colored left borders by severity
       - Verify screenshots are embedded (if any failures occurred)
       - Verify dark mode works (toggle your OS theme)
       - Verify the tone is direct/brutal, not hedging
    5. Check the JSON report: `cat .sniff/reports/report-*.json | head -50`
       - Should be valid JSON with findings array and summary
    6. Run: `npx sniff update-baselines`
       - Should prompt for confirmation
       - Answer 'y' to update
    7. Run `sniff run` again -- visual regression findings should be gone (baselines updated)
  </how-to-verify>
  <resume-signal>Type "approved" if everything works correctly, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A -- verification plan, no new code.

## STRIDE Threat Register

No new threats introduced by verification.
</threat_model>

<verification>
- Full TypeScript compilation passes
- CLI registration correct for all new commands
- All dependencies declared in package.json
- Report generators produce valid output
- Human verifies end-to-end flow works on a real app
</verification>

<success_criteria>
- All 22 Phase 3 requirements verified working
- HTML report visually correct and self-contained
- JSON and JUnit reports valid
- Terminal output matches UI-SPEC
- Human approves the end-to-end experience
</success_criteria>

<output>
After completion, create `.planning/phases/03-browser-runner-quality-scanners-reporting/03-06-SUMMARY.md`
</output>
