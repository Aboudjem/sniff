---
phase: 01-foundation-source-scanner
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified: []
autonomous: false
requirements:
  - CLI-01
  - CLI-02
  - CLI-04
  - CLI-05
  - CLI-06
  - CLI-07
  - SRC-01
  - SRC-02
  - SRC-03
  - SRC-04
  - DIST-04

must_haves:
  truths:
    - "End-to-end CLI workflow works: init -> scan -> report"
    - "Scanner finds real issues in a sample project"
    - "Exit codes behave correctly based on --fail-on threshold"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 1 CLI workflow end-to-end by running sniff against a test fixture directory.

Purpose: Confirm all components are wired correctly and the user experience matches the phase success criteria.
Output: Verified working CLI ready for real-world use.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-source-scanner/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test fixture and run end-to-end verification</name>
  <files>test/fixtures/sample-project/src/app.ts, test/fixtures/sample-project/src/utils.ts, test/fixtures/sample-project/src/broken.ts</files>
  <read_first>src/cli/index.ts, src/cli/commands/scan.ts, src/scanners/source/rules/index.ts</read_first>
  <action>
1. Build the project: `npx tsup`

2. Create test/fixtures/sample-project/src/app.ts containing deliberate issues:
   ```typescript
   // TODO: fix this later
   // FIXME: this is broken
   const apiUrl = 'http://localhost:3000/api';
   console.log('debug output');
   const text = 'Lorem ipsum dolor sit amet';
   export function main() { return text; }
   ```

3. Create test/fixtures/sample-project/src/utils.ts containing:
   ```typescript
   // TBD: decide on implementation
   debugger;
   const testUrl = 'http://127.0.0.1:8080/test';
   export function helper() { return 'ok'; }
   ```

4. Create test/fixtures/sample-project/src/broken.ts containing a broken relative import:
   ```typescript
   import { something } from './nonexistent-module';
   export const value = 'test';
   ```

5. Run end-to-end verification commands from the test fixture directory:
   - `node <project-root>/dist/cli/index.js scan` from the fixtures dir -- verify colored output appears with findings grouped by severity
   - `node <project-root>/dist/cli/index.js scan --json` -- verify valid JSON output (pipe through `node -e "JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'))"`)
   - `node <project-root>/dist/cli/index.js report` -- verify it shows the last scan results
   - `node <project-root>/dist/cli/index.js scan --fail-on info; echo "EXIT: $?"` -- verify exit code is 1 (findings exist at info+ level)
   - `node <project-root>/dist/cli/index.js scan --fail-on critical; echo "EXIT: $?"` -- verify exit code depends on whether critical findings exist

6. Run `node <project-root>/dist/cli/index.js init` from a temp directory -- verify sniff.config.ts is created

7. Verify no Playwright or browser imports leaked in:
   ```bash
   grep -r "playwright" dist/ && echo "FAIL: playwright found" || echo "PASS: no playwright"
   ```
  </action>
  <verify>
    <automated>cd /Users/adamboudj/projects/sniff && npx tsup && cd test/fixtures/sample-project && node /Users/adamboudj/projects/sniff/dist/cli/index.js scan --json 2>&1 | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); console.log('Findings:', d.findings?.length ?? 0); process.exit(d.findings?.length > 0 ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `sniff scan` on fixture dir produces output containing "placeholder-todo" or "TODO" finding
    - `sniff scan` on fixture dir produces output containing "debug-console-log" or "console.log" finding
    - `sniff scan` on fixture dir produces output containing "hardcoded-localhost" finding
    - `sniff scan --json` produces valid JSON parseable by Node.js
    - `sniff report` shows results after a scan has been run
    - `sniff init` in a clean directory creates sniff.config.ts
    - No "playwright" string in dist/ directory
    - Exit code is non-zero when findings exceed fail-on threshold
  </acceptance_criteria>
  <done>All scan rules detect their target patterns, JSON output is valid, report shows persisted results, init creates config, exit codes work correctly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of CLI experience</name>
  <files>none</files>
  <action>Present the built CLI to the user for manual verification of the terminal output quality and overall experience. The automated task above validates correctness; this checkpoint validates the subjective user experience (clean output, modern feel like Vitest/Biome).</action>
  <what-built>Complete Phase 1 CLI: sniff init, sniff scan (with colored output, --json, --fail-on), sniff report. Source scanner detects placeholder text, TODOs, debug artifacts, hardcoded strings, and broken imports.</what-built>
  <how-to-verify>
    1. From the project root, build: `npx tsup`
    2. Run `node dist/cli/index.js --help` -- should show init, scan, run, report commands
    3. Run `node dist/cli/index.js scan` from a real project directory -- should see colored findings grouped by severity (critical/high/medium/low/info)
    4. Run `node dist/cli/index.js scan --json` -- should see valid JSON output
    5. Run `node dist/cli/index.js report` -- should display the last scan results
    6. In a temp directory, run `node <path>/dist/cli/index.js init` -- should create sniff.config.ts with defineConfig template
    7. Verify the terminal output feels clean and modern (like Vitest/Biome per user request)
  </how-to-verify>
  <verify>User confirms the CLI output is clean, modern, and findings are actionable</verify>
  <done>User approves the Phase 1 CLI experience</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification plan -- no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No new components introduced |
</threat_model>

<verification>
- End-to-end scan of test fixtures produces expected findings
- JSON output is valid
- Report displays persisted results
- Init creates config file
- Exit codes work correctly
- No browser dependencies leaked
</verification>

<success_criteria>
Phase 1 success criteria verified:
1. User can run `npx sniff init` and get a valid config file
2. User can run `sniff scan` and see colored terminal output identifying placeholder text, TODOs, hardcoded strings, and broken links
3. User can run `sniff report` to view the last scan results
4. CLI exits with code 0 when no issues exceed severity threshold, non-zero otherwise
5. User can configure sniff via sniff.config.ts, .sniffrc.json, or package.json#sniff
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-source-scanner/01-04-SUMMARY.md`
</output>
